<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="yannick.veron.channelmessaging">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--"@style/AppTheme"-->

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity
            android:name=".ChannelListActivity"
            android:label="ChannelList" >
        </activity>-->

        <activity
            android:name=".MainActivity"
            android:label="MainActivity" >
        </activity>
        <activity
            android:name=".ChannelActivity"
            android:label="ChannelActivity">
        </activity>

        <activity
            android:name=".Frag.DetailActivity"
            android:label="Frag.DetailActivity">
        </activity>
        <activity
            android:name=".MessageFragContainer"
            android:label="MessageFragContainer">
        </activity>
        <activity
            android:name=".FriendList"
            android:label="FriendList">
        </activity>




    </application>

</manifest>